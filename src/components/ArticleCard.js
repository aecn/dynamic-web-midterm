import React from "react";

function ArticleCard ({
    article,
    title,
    content,
}) {
    return (
        <div className="ArticleCard">
            <h2>i want to die</h2>
        </div>
    );
}

export default ArticleCard;
